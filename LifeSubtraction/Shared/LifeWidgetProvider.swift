// 此檔已棄用：LifeProvider / LifeEntry 已內嵌於
//   LifeSubtractionWidget/LifeSubtractionWidget.swift
//
// 之所以保留空檔，是避免在 Xcode 中已經被加進 Project Navigator 的舊參照失效；
// 你可以直接從 Project Navigator 中刪掉此檔（Move to Trash）。
//
// 之後若要加入 watchOS Widget Extension，可在那個 target 自己內嵌一份 Provider，
// 或把這個檔案的 Target Membership 同時設給兩個 widget target（並從那邊移除重複的 Provider）。
